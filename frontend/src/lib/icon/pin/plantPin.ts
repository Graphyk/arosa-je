import { icon } from "leaflet"

export default icon({
  iconUrl: '/images/plantPin.svg',
  iconSize: [42, 42], // largeur, hauteur
  iconAnchor: [16, 32], // point d'ancrage
  popupAnchor: [0, -32]
});